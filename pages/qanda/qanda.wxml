<view class="card">
	<view class="stitle">
		大家都在问
	</view>
	<block wx:for="{{qandalist}}">
		<view class="top_qa">
			<view class="item_q">{{item.topQ}}</view>
			<view class="item_a">{{item.topA}}</view>
		</view>
	</block>
</view>